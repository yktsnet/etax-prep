# 帳簿データを置く GitHub リポジトリ（private）。owner/name 形式。
# アプリ本体とは分けることを推奨する（同一だと1件の入力ごとに Pages が再デプロイされる）。
GITHUB_REPO=
# 上記リポジトリの Contents に read/write を持つ fine-grained token
GITHUB_TOKEN=
# 省略時 main
GITHUB_BRANCH=
# リポジトリ内で帳簿を置くディレクトリ。省略時 ledger
LEDGER_ROOT=
