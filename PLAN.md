# etax-prep PLAN

確定申告（青色65万・e-Tax）の**前準備**を担うWebアプリ。日々の経費入力を最小手数にし、申告時に必要な集計をそのまま出す。e-Taxへの入力自体は手で行うため、本アプリの出口は「転記できる数字」であって提出ではない。

## 前提

給与所得のあるサイドビジネス。費用の計上が主で、売上は月に1度も発生しないことがある。レシート等の紙は封筒で年単位保管し、本アプリは扱わない。クレジット利用のスクショなど電子取引データのみを証憑として保持する。

- 申告区分: 青色65万（複式簿記・貸借対照表あり・e-Tax申告）
- 事業用の口座・クレカを分けていないため、費用の貸方は事業主借に固定する
- 消費税の課税区分は未定。免税・2割特例・本則のいずれも年末に選べる状態でデータを持つ
- 開始は2026年分の途中から。既往分は遡って手入力する

## 完成条件（MVP）

1. **金額と勘定科目のみ**で経費を1件登録できる（日付は当日既定、取引先・摘要は任意）。スマホとPCのどちらからも同等に使える
2. PCからスクショを貼り付けて証憑画像を取引に紐付けられる。スマホからは画像を扱わない
3. 売上を入金日基準で登録できる
4. 登録済みの取引を一覧から探し、**事後に編集・取消できる**。金額・科目・日付・摘要の訂正、証憑画像の追加と削除を含む
5. 入力内容がGitHubリポジトリへ直接コミットされ、cloneすれば帳簿と証憑が完全にローカルへ揃う
6. 給与を専用の設定画面から登録できる。帳簿の入力画面とは分ける
7. ダッシュボードで、勘定科目×月のマトリクスと、家事按分後の年間合計が見られる
8. ダッシュボードで、事業所得と給与所得を合算した課税所得・概算税額・限界税率が見られる
9. 決算書の各行へ転記できる科目別合計を1画面で出せる
10. 年末の決算整理（年をまたぐ未収・未払の拾い上げ）を案内する画面がある

## スコープ外

- e-Taxへの自動入力・電子申告（確定申告書等作成コーナーで手入力する）
- 紙のレシート・領収書の電子化（封筒で原本保管する。スキャナ保存制度は採らない）
- クレジット・銀行明細のCSV取込、レシート写真のOCR
- 正規の減価償却（固定資産台帳・耐用年数別の償却計算）。一括償却と少額特例のみ扱う
- 複数事業者・複数ユーザー対応

## 主要な仕様

### 入力

勘定科目は決算書の科目名をそのまま出す。実際に使うものだけに絞り、使わない科目は既定で隠して `config` で足せるようにする。並びは直近使用順。

接待交際費を選んだときだけ、相手と目的の欄を出す。この科目は記録がないと否認されやすく、後から思い出せないため。

分類に迷った時点で入力を止めないことを優先し、雑費へ入れて送信できるようにする。未分類の再割当ては後からまとめて行う。

スマホとPCで入力の質を変えない。項目・操作手順は共通にする。証憑画像はPCからの貼り付けのみで扱い、スマホからは追加しない。電子取引データはPCで発生するため、スマホ側に画像経路を持たせても使われない。

### 編集と取消

打ち間違いは日常的に起きるため、事後の訂正経路を最初から用意する。一覧から取引を探し、金額・科目・日付・摘要を直せる。証憑画像は後から足すことも外すこともできる（PCから）。

取引の削除は行を消さず、取消フラグを立てて集計から外す。

### 複式化

入力は借方の勘定科目のみを受け取り、貸方は事業主借で固定して仕訳を生成する。貸借対照表は事業主借・事業主貸・元入金・売掛金・未払金で構成する。

### 消費税

金額は税込1本で保持し、各エントリに税率区分（10% / 8% / 対象外）を科目別の既定値から自動付与する。既定が当たるため入力時の手数は増えず、年末にどの課税方式を選んでも計算し直せる。

### 家事按分

科目ごとに既定の按分率を持ち、エントリ単位で上書きできる。集計は按分後の額で行う。

### 資産

10万円以上の入力時に、一括償却（3年均等）と少額特例（30万円未満）の判定補助を出す。

### 給与

給与は帳簿へ混ぜず、所得区分を分けたまま合算する。入力画面も分け、日々の経費入力とは別の設定画面で扱う。

給与は基本的に固定であり、開始年月と振込日を指定して月額を登録する。昇給や賞与で額が変わるため、単一の値ではなく**適用開始年月付きの履歴**として持ち、集計時はその年月時点で有効な値を引く。過去に遡って直す場合も履歴の追加で表現する。

振込日を持つのは、月次の水位を見るときに給与の入る日を実態に合わせるため。給与所得の年間集計自体は振込日に左右されないが、資金の動きとしては振込日が意味を持つ。

年末には源泉徴収票一式（支払金額・源泉徴収税額・社会保険料等・年末調整済みの控除）で確定値へ置き換える。期中は登録した月額からの見込み、年末以降は確定値、という扱いにする。

### 保存

GitHubのprivateリポジトリを正本とする。仕訳データと証憑画像をいずれもgit管理下に置き、cloneひとつで帳簿と証憑が揃う状態を保つ。git履歴が訂正削除の記録を兼ねる。

## 未決事項

- 静的サイト生成の有無（ビルド時集計とするか、Functionsで都度集計するか）
- 証憑画像のファイル名規則（取引年月日・金額・取引先を含めて検索要件の代替とする方向）
- 既往分の遡り一括入力の入口の形
- 電子取引データの改ざん防止に用いる事務処理規程の置き場所
